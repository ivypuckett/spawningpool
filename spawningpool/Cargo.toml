[package]
name = "spawningpool"
version = "0.1.0"
edition = "2021"
description = "Create hyper-specific, 0-waste agents"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
regex = "1"
